{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open in Browser (localhost:5173)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "dev:all (concurrently)"
    }
  ]
}